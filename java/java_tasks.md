markdown
# Практика Java

Этот проект содержит набор задач по Java для практики программирования.

## Структура проекта
- `Main.java` - основной файл для написания решений и тестирования
- `java_tasks.md` - файл с задачами разных уровней сложности
- `README.md` - этот файл

## Как работать
1. Откройте файл `java_tasks.md` и выберите задачу для решения
2. Реализуйте решение в файле `Main.java`
3. Протестируйте решение в методе `main()`
4. Сообщите мне номер решенной задачи для проверки

## Уровни сложности
- **Уровень 1**: Основы синтаксиса
- **Уровень 2**: Массивы и строки
- **Уровень 3**: Классы и объекты
- **Уровень 4**: Алгоритмы и коллекции
- **Уровень 5**: Продвинутые темы

Удачи в решении задач! 🚀
2. Файл java_tasks.md:
markdown
# Задачи по Java для практики

## Уровень 1: Основы синтаксиса

### Задача 1.1: Сумма двух чисел
Напишите метод `sum(int a, int b)`, который возвращает сумму двух целых чисел.

**Пример:**
sum(5, 3) // должно вернуть 8
sum(-1, 1) // должно вернуть 0

text

---

### Задача 1.2: Максимум из двух чисел
Напишите метод `max(int a, int b)`, который возвращает большее из двух чисел.

**Пример:**
max(5, 3) // должно вернуть 5
max(-1, 1) // должно вернуть 1

text

---

### Задача 1.3: Четное или нечетное
Напишите метод `isEven(int n)`, который возвращает `true`, если число четное, и `false` если нечетное.

**Пример:**
isEven(4) // должно вернуть true
isEven(5) // должно вернуть false

text

---

### Задача 1.4: Факториал
Напишите метод `factorial(int n)`, который вычисляет факториал числа n (n! = 1 * 2 * 3 * ... * n).

**Пример:**
factorial(5) // должно вернуть 120
factorial(0) // должно вернуть 1

text

---

### Задача 1.5: Сумма элементов массива
Напишите метод `sumArray(int[] arr)`, который возвращает сумму всех элементов массива.

**Пример:**
sumArray(new int[]{1, 2, 3, 4, 5}) // должно вернуть 15
sumArray(new int[]{}) // должно вернуть 0

text

---

## Уровень 2: Массивы и строки

### Задача 2.1: Максимум в массиве
Напишите метод `maxInArray(int[] arr)`, который возвращает максимальный элемент в массиве. Если массив пустой, верните `Integer.MIN_VALUE`.

**Пример:**
maxInArray(new int[]{3, 7, 2, 9, 1}) // должно вернуть 9
maxInArray(new int[]{-5, -2, -10}) // должно вернуть -2

text

---

### Задача 2.2: Реверс массива
Напишите метод `reverseArray(int[] arr)`, который возвращает новый массив с элементами в обратном порядке.

**Пример:**
reverseArray(new int[]{1, 2, 3, 4, 5}) // должно вернуть new int[]{5, 4, 3, 2, 1}

text

---

### Задача 2.3: Поиск элемента
Напишите метод `findIndex(int[] arr, int target)`, который возвращает индекс первого вхождения элемента target в массиве. Если элемент не найден, верните -1.

**Пример:**
findIndex(new int[]{1, 2, 3, 4, 5}, 3) // должно вернуть 2
findIndex(new int[]{1, 2, 3, 4, 5}, 6) // должно вернуть -1

text

---

### Задача 2.4: Подсчет символов в строке
Напишите метод `countChars(String s)`, который возвращает `Map<Character, Integer>` - карту, где ключ - символ, значение - количество его вхождений в строку.

**Пример:**
countChars("hello") // должно вернуть Map с {'h':1, 'e':1, 'l':2, 'o':1}

text

---

### Задача 2.5: Проверка палиндрома
Напишите метод `isPalindrome(String s)`, который проверяет, является ли строка палиндромом (читается одинаково слева направо и справа налево). Игнорируйте регистр и пробелы.

**Пример:**
isPalindrome("racecar") // должно вернуть true
isPalindrome("hello") // должно вернуть false
isPalindrome("A man a plan a canal Panama") // должно вернуть true

text

---

## Уровень 3: Классы и объекты

### Задача 3.1: Класс Point
Создайте класс `Point` с полями `x` и `y` типа `double`. Реализуйте:
- Конструктор `Point(double x, double y)`
- Метод `distance(Point other)`, который вычисляет расстояние до другой точки по формуле: √((x₂-x₁)² + (y₂-y₁)²)

**Пример:**
Point p1 = new Point(0, 0);
Point p2 = new Point(3, 4);
p1.distance(p2) // должно вернуть 5.0

text

---

### Задача 3.2: Класс Rectangle
Создайте класс `Rectangle` с полями `width` и `height` типа `double`. Реализуйте:
- Конструктор `Rectangle(double width, double height)`
- Метод `area()` - площадь прямоугольника
- Метод `perimeter()` - периметр прямоугольника

**Пример:**
Rectangle r = new Rectangle(5, 3);
r.area() // должно вернуть 15.0
r.perimeter() // должно вернуть 16.0

text

---

### Задача 3.3: Класс Counter
Создайте класс `Counter` с полем `value` типа `int`. Реализуйте:
- Конструктор `Counter()` - инициализирует `value = 0`
- Метод `increment()` - увеличивает `value` на 1
- Метод `decrement()` - уменьшает `value` на 1
- Метод `getValue()` - возвращает текущее значение

**Пример:**
Counter c = new Counter();
c.increment();
c.increment();
c.getValue() // должно вернуть 2
c.decrement();
c.getValue() // должно вернуть 1

text

---

## Уровень 4: Алгоритмы и коллекции

### Задача 4.1: Проверка на простое число
Напишите метод `isPrime(int n)`, который проверяет, является ли число простым.

**Пример:**
isPrime(7) // должно вернуть true
isPrime(10) // должно вернуть false
isPrime(1) // должно вернуть false

text

---

### Задача 4.2: Числа Фибоначчи
Напишите метод `fibonacci(int n)`, который возвращает n-е число Фибоначчи (F(0)=0, F(1)=1, F(n)=F(n-1)+F(n-2)).

**Пример:**
fibonacci(0) // должно вернуть 0
fibonacci(1) // должно вернуть 1
fibonacci(7) // должно вернуть 13

text

---

### Задача 4.3: Бинарный поиск
Напишите метод `binarySearch(int[] arr, int target)`, который ищет элемент в отсортированном массиве и возвращает его индекс. Если элемент не найден, верните -1.

**Пример:**
binarySearch(new int[]{1, 3, 5, 7, 9}, 5) // должно вернуть 2
binarySearch(new int[]{1, 3, 5, 7, 9}, 4) // должно вернуть -1

text

---

### Задача 4.4: Удаление дубликатов из ArrayList
Напишите метод `removeDuplicates(List<Integer> list)`, который возвращает новый список без дубликатов, сохраняя порядок первого вхождения каждого элемента.

**Пример:**
List<Integer> input = Arrays.asList(1, 2, 2, 3, 3, 3, 4);
removeDuplicates(input) // должно вернуть [1, 2, 3, 4]

text

---

### Задача 4.5: Самое частое слово
Напишите метод `mostFrequentWord(String[] words)`, который возвращает слово, которое встречается чаще всего. Если таких слов несколько, верните первое по алфавиту.

**Пример:**
mostFrequentWord(new String[]{"apple", "banana", "apple", "cherry", "banana", "apple"})
// должно вернуть "apple"

text

---

## Уровень 5: Продвинутые темы

### Задача 5.1: Группировка по четности
Напишите метод `groupByParity(int[] arr)`, который возвращает `Map<String, List<Integer>>` с ключами "even" и "odd", содержащими соответствующие числа.

**Пример:**
groupByParity(new int[]{1, 2, 3, 4, 5, 6})
// должно вернуть Map с {"even": [2, 4, 6], "odd": [1, 3, 5]}

text

---

### Задача 5.2: Сортировка пузырьком
Напишите метод `bubbleSort(int[] arr)`, который сортирует массив методом пузырька (изменяет исходный массив).

**Пример:**
int[] arr = {64, 34, 25, 12, 22, 11, 90};
bubbleSort(arr);
// arr должно стать {11, 12, 22, 25, 34, 64, 90}

text

---

### Задача 5.3: Объединение двух отсортированных массивов
Напишите метод `mergeSorted(int[] arr1, int[] arr2)`, который объединяет два отсортированных массива в один отсортированный массив.

**Пример:**
mergeSorted(new int[]{1, 3, 5}, new int[]{2, 4, 6})
// должно вернуть new int[]{1, 2, 3, 4, 5, 6}

text

---

### Задача 5.4: Класс BankAccount
Создайте класс `BankAccount` с полями:
- `String accountNumber`
- `double balance` (приватное поле)

Реализуйте:
- Конструктор `BankAccount(String accountNumber, double initialBalance)`
- Метод `deposit(double amount)` - пополнение счета
- Метод `withdraw(double amount)` - снятие денег (проверять, что balance >= amount)
- Метод `getBalance()` - получение баланса
- Метод `getAccountNumber()` - получение номера счета

---

### Задача 5.5: Класс Student с наследованием
Создайте базовый класс `Person` с полями `String name` и `int age`.
Создайте класс `Student`, который наследуется от `Person` и добавляет поле `String studentId`.
Реализуйте конструкторы и методы `getName()`, `getAge()`, `getStudentId()`.

---

## Инструкции по решению
1. Создайте файл `Main.java` (или любой другой .java файл)
2. Напишите решение задачи в этом файле
3. Сообщите мне номер задачи, которую вы решили
4. Я проверю ваше решение и дам обратную связь

**Важно для Java:**
- Имя класса должно совпадать с именем файла
- Все должно быть внутри класса
- Используйте `public static` для методов, которые можно вызывать без создания объекта

**Пример структуры:**
```java
public class Main {
    public static void main(String[] args) {
        // тестирование вашего решения
    }
    
    // здесь ваши методы
    public static int sum(int a, int b) {
        return a + b;
    }
}
Удачи! 🚀

text

## 3. Файл `Main.java`:
```java
public class Main {
    public static void main(String[] args) {
        System.out.println("Добро пожаловать! Решайте задачи из файла java_tasks.md");
        
        // Здесь можете тестировать свои решения
        System.out.println("sum(5, 3) = " + sum(5, 3)); // должно быть 8
    }
    
    // Здесь вы будете писать решения задач
    
    // Пример: Задача 1.1
    public static int sum(int a, int b) {
        return a + b;
    }
    
    // ВАШИ РЕШЕНИЯ БУДУТ ЗДЕСЬ ↓
    // Добавляйте новые методы под этим комментарием
}